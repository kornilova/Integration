<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="street">

    <insert id="insert" keyColumn="CITY_ID" parameterType="com.estylesoft.integration.Model.Ptks.StreetModel">
        <selectKey keyProperty="id" resultType="Long" order="BEFORE">
            select GET_NEW_ID() from sysibm.SYSDUMMY1
        </selectKey>
        insert into STH_STREET(STREET_ID, CITY_ID, TOWN_ID, TER_ID, RGN_ID, STREET_NAME, STREET_KLADR_CODE, RDV_ID)
        values (
        #{id,javaType=Long,jdbcType=BIGINT},
        #{city.id,javaType=Long,jdbcType=BIGINT},
        #{town.id,javaType=Long,jdbcType=BIGINT},
        #{admTerritory.id,javaType=Long,jdbcType=BIGINT},
        #{region.id,javaType=Long,jdbcType=BIGINT},
        #{name},
        #{code},
        #{baseDepartment.id,javaType=Long,jdbcType=BIGINT}
        )
    </insert>

    <delete id="delete" parameterType="java.lang.Long">
        delete from STH_STREET
        where STREET_KLADR_CODE = #{code}
    </delete>

    <select id="getByCodeCityIdTownIdAdmTerIdRegionIdBaseDepId" parameterType="Map" resultType="com.estylesoft.integration.Model.Ptks.StreetModel">
          SELECT STREET_ID as id, STREET_KLADR_CODE as code, STREET_NAME as name
          FROM STH_STREET
          WHERE STREET_KLADR_CODE = #{code}
          <if test="townId != null">
            and TOWN_ID = #{townId}
          </if>
          <if test="cityId != null">
            and  CITY_ID = #{cityId}
          </if>
          <if test="admTerritoryId != null">
            and  TER_ID = #{admTerritoryId}
          </if>
          <if test="regionId != null">
            and RGN_ID = #{regionId}
          </if>
          <if test="baseDepartmentId != null">
            and RDV_ID = #{baseDepartmentId}
          </if>
    </select>

</mapper>
